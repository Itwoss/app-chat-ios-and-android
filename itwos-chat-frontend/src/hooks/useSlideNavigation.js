/**
 * Stub for slide/swipe navigation. No-op when skip is true or by default.
 */
export function useSlideNavigation(_opts = {}) {
  // No-op: swipe navigation disabled or handled elsewhere
}

export default useSlideNavigation
